# Drizzle migrations

Generated from `src/server/db/schema.ts` via `npm run db:generate`.

- Prefer **`npm run db:push`** for local/dev apply.
- Use these SQL files when you need a reviewable / versioned migration history.
- Bootstrap dump kept in sync: `scripts/schema.sql`.
